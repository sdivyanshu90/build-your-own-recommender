name: Container verification
on:
  pull_request: {paths: [Dockerfile, docker-compose.yml, pyproject.toml, uv.lock, "src/**"]}
  workflow_dispatch:
permissions: {contents: read, packages: write}
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: two-tower-recommender:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - run: docker image inspect two-tower-recommender:test
